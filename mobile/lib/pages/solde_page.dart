import 'package:flutter/cupertino.dart';

class SoldePage extends StatelessWidget {
  const SoldePage({super.key});

   @override
  Widget build(BuildContext context) {
    return const CupertinoPageScaffold(
      child: Center(child: Text("Solde"))
    );
  }
}
